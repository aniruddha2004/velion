package com.example.personal_news_assistant

import io.flutter.embedding.android.FlutterActivity

class MainActivity: FlutterActivity() {
    // receive_sharing_intent plugin handles everything automatically
    // No custom code needed - the plugin registers itself via Flutter embedding
}
